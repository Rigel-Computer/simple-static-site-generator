
# 09.06.2025 22:10 – Ergänzung um Rewrite auf static-copy/TestFinal (erstellt von ChatGPT, keine Gewähr)

# Aktiviert mod_rewrite, falls noch nicht geschehen
RewriteEngine On

# Nur umleiten, wenn Zielordner existiert (Failsafe)
RewriteCond %{DOCUMENT_ROOT}/static-copy/TestFinal -d

# Nur weiterleiten, wenn Datei oder Verzeichnis NICHT existiert
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Umleitung an das Unterverzeichnis
RewriteRule ^(.*)$ static-copy/TestFinal/$1 [L]

# 09.06.2025 22:10 – Ergänzung um Rewrite auf static-copy/TestFinal (erstellt von ChatGPT, keine Gewähr)
